// The app's version, shown in Settings and used for the "check for updates"
// comparison against the latest GitHub release. Keep in sync with package.json
// / Cargo.toml / tauri.conf.json on every release.
export const APP_VERSION = "3.3.0";
